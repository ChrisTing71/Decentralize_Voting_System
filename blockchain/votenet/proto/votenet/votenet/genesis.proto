syntax = "proto3";
package votenet.votenet;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "votenet/votenet/params.proto";

option go_package = "votenet/x/votenet/types";

// GenesisState defines the votenet module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
